<div class="Activities_List">
  <% @activities.each do |activity| %>
  <%= link_to park_activity_park_activity(activity.id), :action => 'go'  do %>
    <div class="Container">
      <h3><%= activity.name %><h3>
    <% end %>
  <% end %>
  </div>
</div>
